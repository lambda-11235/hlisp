
* Primitive functions should not be treated as special forms. As they are now,
   they cannot be passed to other functions.
* Add syntax for cons cells (ie. ('a . 'b)).
* Formalize the specification, include more detail.